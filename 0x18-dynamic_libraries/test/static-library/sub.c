/**
 * @sub - takes two numbers and subtracts b from a 
 * @a: first integer to subtract from
 * @b: second integer to subtract from @a
 * 
 * @return result of @a - @b 
 */
int sub(int a, int b)
{
	return (a - b);
}